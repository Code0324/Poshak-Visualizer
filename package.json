{
  "name": "poshak-visualizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npm --prefix frontend start",
    "dev": "npm --prefix frontend run dev"
  }
}
